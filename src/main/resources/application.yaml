app:
  jwt:
    secret-key: ${JWT_SECRET:Aq6pZgd05v69f4af91ma43541U0f5fb6Z2dc23}
    expiration-time: 900000 # 15 minutes

server:
  port: 9099

spring:
  application:
    name: Mfa Application
  datasource:
    url: jdbc:postgresql://localhost:5435/mfa_app_db
    username: tountoun
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      show-sql: false

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
  info:
    title: MFA Application
    version: 1.0.0
    description: Spring Boot MFA compatible Google Authenticator / Authy